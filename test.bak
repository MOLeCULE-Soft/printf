#include "main.h"
#include <stdio.h>
#include <limits.h>
int main(void)
{

	_printf("Octal of %u is %o\n", 345, 345);
	_printf("Octal of %u is %o\n", (unsigned int)INT_MAX + 1024, (unsigned int)INT_MAX + 1024);
        return (0);
}
